<h1> NOOBS ;)</h1>


<div id="map-container"></div>

<script>
  function init() {
    const mapContainerElement = document.getElementById('map-container');
    const map = new window.google.maps.Map(mapContainerElement, {
      center: {
        lat: 38.712783,
        lng: -9.138872
      },
      zoom: 13
    });


    {{!-- const latitude = {{ bars.[0].location.coordinates.[0] }};
    const longitude = {{ bars.[0].location.coordinates.[1] }}; --}} 

    const bars = {{{ json bars }}};
    for(let bar of bars){
    const marker = new window.google.maps.Marker({
      map,
      icon: 'https://i.ibb.co/7KJvLCc/Webp-net-resizeimage-1.png',

      position: {
        lat: bar.location.coordinates[0],
        lng: bar.location.coordinates[1]
      }
    });
    }


    
  }
</script>
{{>mapscript}}